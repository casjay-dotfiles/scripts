#!/usr/bin/env bash
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
##@Version       : 202108190117-git
# @Author        : Jason Hempstead
# @Contact       : jason@casjaysdev.com
# @License       : WTFPL
# @ReadME        : run_server --help
# @Copyright     : Copyright: (c) 2021 Jason Hempstead, Casjays Developments
# @Created       : Thursday, Aug 19, 2021 01:17 EDT
# @File          : run_server
# @Description   : Start an HTTP server from a directory
# @TODO          :
# @Other         :
# @Resource      :
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
APPNAME="$(basename "$0" 2>/dev/null)"
VERSION="202108190117-git"
HOME="${USER_HOME:-$HOME}"
USER="${SUDO_USER:-$USER}"
RUN_USER="${SUDO_USER:-$USER}"
SRC_DIR="${BASH_SOURCE%/*}"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Set bash options
if [[ "$1" == "--debug" ]]; then shift 1 && set -xo pipefail && export SCRIPT_OPTS="--debug" && export _DEBUG="on"; fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Import functions
CASJAYSDEVDIR="${CASJAYSDEVDIR:-/usr/local/share/CasjaysDev/scripts}"
SCRIPTSFUNCTDIR="${CASJAYSDEVDIR:-/usr/local/share/CasjaysDev/scripts}/functions"
SCRIPTSFUNCTFILE="${SCRIPTSAPPFUNCTFILE:-testing.bash}"
SCRIPTSFUNCTURL="${SCRIPTSAPPFUNCTURL:-https://github.com/dfmgr/installer/raw/main/functions}"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
if [ -f "$PWD/$SCRIPTSFUNCTFILE" ]; then
  . "$PWD/$SCRIPTSFUNCTFILE"
elif [ -f "$SCRIPTSFUNCTDIR/$SCRIPTSFUNCTFILE" ]; then
  . "$SCRIPTSFUNCTDIR/$SCRIPTSFUNCTFILE"
else
  echo "Can not load the functions file: $SCRIPTSFUNCTDIR/$SCRIPTSFUNCTFILE" 1>&2
  exit 1
fi
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# user system devenv dfmgr dockermgr fontmgr iconmgr pkmgr systemmgr thememgr wallpapermgr
user_install
__options "$@"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Set functions
__list_options() { printf_custom "$1" "$2: $(echo ${3:-$ARRAY} | __sed 's|:||g;s|'$4'| '$5'|g')" 2>/dev/null; }
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
__exit() {
  local pid="$1"
  trap 'trap - INT
  kill -s INT "$pid"
' INT
  printf_red "Killing the server with pid: $pid"
  exit 0
}
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
__gen_config() {
  [[ "$INIT_CONFIG" = "TRUE" ]] || printf_green "Generating the config file in"
  [[ "$INIT_CONFIG" = "TRUE" ]] || printf_green "$RUN_SERVER_CONFIG_DIR/$RUN_SERVER_CONFIG_FILE"
  [ -d "$RUN_SERVER_CONFIG_DIR" ] || mkdir -p "$RUN_SERVER_CONFIG_DIR"
  [ -d "$RUN_SERVER_CONFIG_BACKUP_DIR" ] || mkdir -p "$RUN_SERVER_CONFIG_BACKUP_DIR"
  [ -f "$RUN_SERVER_CONFIG_DIR/$RUN_SERVER_CONFIG_FILE" ] &&
    cp -Rf "$RUN_SERVER_CONFIG_DIR/$RUN_SERVER_CONFIG_FILE" "$RUN_SERVER_CONFIG_BACKUP_DIR/$RUN_SERVER_CONFIG_FILE.$$"
  cat <<EOF >"$RUN_SERVER_CONFIG_DIR/$RUN_SERVER_CONFIG_FILE"
# Settings for run_server

# Notification settings
RUN_SERVER_GOOD_MESSAGE="${RUN_SERVER_GOOD_MESSAGE:-Everything Went OK}"
RUN_SERVER_ERROR_MESSAGE="${RUN_SERVER_ERROR_MESSAGE:-Well something seems to have gone wrong}"
RUN_SERVER_NOTIFY_ENABLED="${RUN_SERVER_NOTIFY_ENABLED:-yes}"
RUN_SERVER_NOTIFY_CLIENT_NAME="${NOTIFY_CLIENT_NAME:-$APPNAME}"
RUN_SERVER_NOTIFY_CLIENT_ICON="${NOTIFY_CLIENT_ICON:-$RUN_SERVER_NOTIFY_CLIENT_ICON}"

# Colorization settings
RUN_SERVER_OUTPUT_COLOR="${RUN_SERVER_OUTPUT_COLOR:-5}"
RUN_SERVER_OUTPUT_COLOR_GOOD="${RUN_SERVER_OUTPUT_COLOR_GOOD:-2}"
RUN_SERVER_OUTPUT_COLOR_ERROR="${RUN_SERVER_OUTPUT_COLOR_ERROR:-1}"

EOF
  if [ -f "$RUN_SERVER_CONFIG_DIR/$RUN_SERVER_CONFIG_FILE" ]; then
    [[ "$INIT_CONFIG" = "TRUE" ]] || printf_green "Your config file for $APPNAME has been created"
    exitCode=0
  else
    printf_red "Failed to create the config file"
    exitCode=1
  fi
  return ${exitCode:-$?}
}

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Additional functions
main() {
  trap '__exit $$' INT
  i=0
  port="${1:-8000}" && shift 1
  other_port="${1:-}"
  prog="${prog:-python2}"
  myip=$(__getlipaddr && echo "$CURRIP4" || echo '127.0.0.1')
  myhost="$(hostname -s || echo localhost)"
  MAX_NUMBER_OF_TRIES=${MAX_NUMBER_OF_TRIES:-20}
  RUN_SERVER_TEMP_DIR="${RUN_SERVER_TEMP_DIR:-/tmp}"
  # - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
  if ! __test_server "${other_port:-$port}"; then
    printf_red "Something appears to be running on port ${other_port:-$port}"
    printf_yellow "Attempting the next port: $((port + 1))"
    while :; do
      [ -n "$other_port" ] && other_port=$((other_port + 1)) || other_port=$((port + 1))
      __test_server "${other_port:-$port}" || break
    done
    port=$other_port
  fi
  SERVER_LOG="${SERVER_LOG:-$RUN_SERVER_TEMP_DIR/run_server_${other_port:-$port}.log}"
  printf '%s\n' "Starting the server on port ${other_port:-$port}" &>"$SERVER_LOG"
  while [ $i -lt $MAX_NUMBER_OF_TRIES ]; do
    if [ "$(lsof -i -nP | grep "${other_port:-$port}" | grep -i "$prog")" != "" ]; then
      sleep 1
      open_url "http://$myip:${other_port:-$port}"
      break
    fi
    i=$((i + 1))
    sleep .5
  done &
  printf_purple "serving files from $(realpath "${RUN_SERVER_CWD:-$PWD}" 2>/dev/null) with pid: $$"
  printf_blue "Log file: $SERVER_LOG"
  printf_yellow "Access from the network: http://$myip:${other_port:-$port}"
  printf_red "control-c to exit"
}
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
__test_server() { netstatg LISTEN | grep -q ":${1:$port}" && return 1 || return 0; }
__return_error() { printf '%s\n' "Failed to start server" &>>"$SERVER_LOG" && printf_exit 1 1 "Failed to start the server"; }
__netcat_serv() { while true; do nc -l "${port}" <.; done &>>"$SERVER_LOG" && echo netcat started &>>"$SERVER_LOG" || return 1; }
__return_error() { printf '%s\n' "Failed to start server" &>>"$SERVER_LOG" && printf_exit 1 1 "Failed to start the server"; }
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
__go() { cmd_exists gohttpserver && cd "$RUN_SERVER_CWD" && gohttpserver --theme dark --port ${port} --title="Dev Server" -r "." --xheaders --cors &>>"$SERVER_LOG" && echo gohttpserver started &>>"$SERVER_LOG" || return 1; }
__caddy() { cmd_exists caddy && cd "$RUN_SERVER_CWD" && caddy file-server -browse -listen :${port} -root "." -templates &>>"$SERVER_LOG" && echo caddy started &>>"$SERVER_LOG" || return 1; }
__proxy() { cmd_exists caddy && cd "$RUN_SERVER_CWD" && caddy reverse-proxy -to 127.0.0.1:${port:-3000} -from :${other_port:-20000} &>>"$SERVER_LOG" && echo caddy started &>>"$SERVER_LOG" && other_port="${other_port:-20000}" || return 1; }
__jekyll() { cmd_exists jekyll && cd "$RUN_SERVER_CWD" && jekyll serve -P ${port} -l -H 0.0.0.0 &>>"$SERVER_LOG" && echo jekyll started &>>"$SERVER_LOG" || return 1; }
__js_serv() { cmd_exists http-server && cd "$RUN_SERVER_CWD" && http-server -a 0.0.0.0 -p ${port} &>>"$SERVER_LOG" && echo http-server started &>>"$SERVER_LOG" || return 1; }
__php_serv() { cmd_exists php && cd "$RUN_SERVER_CWD" && php -S 0.0.0.0:${port} &>>"$SERVER_LOG" && echo php started &>>"$SERVER_LOG" || return 1; }
__ruby_serv() { cmd_exists ruby && cd "$RUN_SERVER_CWD" && ruby -run -ehttpd -p ${port} &>>"$SERVER_LOG" && echo ruby started &>>"$SERVER_LOG" || return 1; }
__rails_serv() { cmd_exists rails && cd "$RUN_SERVER_CWD" && server -p "$port" &>>"$SERVER_LOG" && echo ruby started &>>"$SERVER_LOG" || return 1; }
__python3_serv() { cmd_exists python3 && cd "$RUN_SERVER_CWD" && python3 -m http.server --cgi --bind "$myip" "${port}" &>>"$SERVER_LOG" && echo Py3 started &>>"$SERVER_LOG" || return 1; }
__python2_serv() { cmd_exists python2 && cd "$RUN_SERVER_CWD" && python2 -m http.server --cgi --bind "$myip" "${port}" &>>"$SERVER_LOG" && echo Py2 started &>>"$SERVER_LOG" || return 1; }
__py_simple() { cmd_exists python && cd "$RUN_SERVER_CWD" && python -m SimpleHTTPServer "${port}" &>>"$SERVER_LOG" && echo "Py simple start" &>>"$SERVER_LOG" || return 1; }
__default_srv() { cmd_exists python2 && cd "$RUN_SERVER_CWD" && python2 -c $'import SimpleHTTPServer;\nmap = SimpleHTTPServer.SimpleHTTPRequestHandler.extensions_map;\nmap[""] = "text/plain";\nfor key, value in map.items():\n\tmap[key] = value + ";charset=UTF-8";\nSimpleHTTPServer.test();' "${port}" &>>"$SERVER_LOG" && echo "Default has started" || return 1; }
__netcat_serv() { cd "$RUN_SERVER_CWD" && while true; do nc -l "${port}" <.; done &>>"$SERVER_LOG" && echo netcat started &>>"$SERVER_LOG" || return 1; }
__static() { __go || __caddy || __php_serv || return 1; }
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
open_url() {
  [ -n "$SSH_CONNECTION" ] && return
  if cmd_exists mybrowser; then
    mybrowser "$1"
  elif cmd_exists "xdg-open"; then
    xdg-open "$1"
  elif cmd_exists "open"; then
    open "$1"
  fi
}
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Default variables
exitCode="0"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Application Folders
RUN_SERVER_LOG_DIR="${RUN_SERVER_LOG_DIR:-$HOME/.local/log/run_server}"
RUN_SERVER_CACHE_DIR="${RUN_SERVER_CACHE_DIR:-$HOME/.cache/run_server}"
RUN_SERVER_CONFIG_DIR="${RUN_SERVER_CONFIG_DIR:-$HOME/.config/myscripts/run_server}"
RUN_SERVER_CONFIG_BACKUP_DIR="${RUN_SERVER_CONFIG_BACKUP_DIR:-$HOME/.local/share/myscripts/run_server/backups}"
RUN_SERVER_TEMP_DIR="${RUN_SERVER_TEMP_DIR:-$HOME/.local/tmp/system_scripts/run_server}"
RUN_SERVER_OPTIONS_DIR="${RUN_SERVER_OPTIONS_DIR:-$HOME/.local/share/myscripts/run_server/options}"
RUN_SERVER_CONFIG_FILE="${RUN_SERVER_CONFIG_FILE:-settings.conf}"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Color Settings
RUN_SERVER_OUTPUT_COLOR="${RUN_SERVER_OUTPUT_COLOR:-4}"
RUN_SERVER_OUTPUT_COLOR_2="${RUN_SERVER_OUTPUT_COLOR:-6}"
RUN_SERVER_OUTPUT_COLOR_GOOD="${RUN_SERVER_OUTPUT_COLOR_GOOD:-2}"
RUN_SERVER_OUTPUT_COLOR_ERROR="${RUN_SERVER_OUTPUT_COLOR_ERROR:-1}"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Notification Settings
RUN_SERVER_GOOD_MESSAGE="${RUN_SERVER_GOOD_MESSAGE:-Everything Went OK}"
RUN_SERVER_ERROR_MESSAGE="${RUN_SERVER_ERROR_MESSAGE:-Well something seems to have gone wrong}"
RUN_SERVER_NOTIFY_ENABLED="${RUN_SERVER_NOTIFY_ENABLED:-yes}"
RUN_SERVER_NOTIFY_CLIENT_NAME="${NOTIFY_CLIENT_NAME:-$APPNAME}"
RUN_SERVER_NOTIFY_CLIENT_ICON="${NOTIFY_CLIENT_ICON:-$RUN_SERVER_NOTIFY_CLIENT_ICON}"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Application overrides

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Generate non-existing config files
[ -f "$RUN_SERVER_CONFIG_DIR/$RUN_SERVER_CONFIG_FILE" ] || [[ "$*" = *config ]] || INIT_CONFIG="${INIT_CONFIG:-TRUE}" __gen_config ${SETARGS:-$@}
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Import config
[ -f "$RUN_SERVER_CONFIG_DIR/$RUN_SERVER_CONFIG_FILE" ] && . "$RUN_SERVER_CONFIG_DIR/$RUN_SERVER_CONFIG_FILE"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Ensure Directories and files exist
[ -d "$RUN_SERVER_LOG_DIR" ] || mkdir -p "$RUN_SERVER_LOG_DIR" &>/dev/null
[ -d "$RUN_SERVER_TEMP_DIR" ] || mkdir -p "$RUN_SERVER_TEMP_DIR" &>/dev/null
[ -d "$RUN_SERVER_CACHE_DIR" ] || mkdir -p "$RUN_SERVER_CACHE_DIR" &>/dev/null
RUN_SERVER_TEMP_FILE="${RUN_SERVER_TEMP_FILE:-$(mktemp $RUN_SERVER_TEMP_DIR/XXXXXX 2>/dev/null)}"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Setup trap to remove temp file
trap 'exitCode=${exitCode:-$?};[ -n "$RUN_SERVER_TEMP_FILE" ] && [ -f "$RUN_SERVER_TEMP_FILE" ] && rm -Rf "$RUN_SERVER_TEMP_FILE" &>/dev/null' EXIT
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Setup notification function
if [ "$RUN_SERVER_NOTIFY_ENABLED" = "yes" ]; then
  __notifications() {
    export NOTIFY_GOOD_MESSAGE="${RUN_SERVER_GOOD_MESSAGE}"
    export NOTIFY_ERROR_MESSAGE="${RUN_SERVER_ERROR_MESSAGE}"
    export NOTIFY_CLIENT_NAME="${RUN_SERVER_NOTIFY_CLIENT_NAME}"
    export NOTIFY_CLIENT_ICON="${RUN_SERVER_NOTIFY_CLIENT_ICON}"
    notifications "$@" || return 1
  }
else
  __notifications() { false; }
fi
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Show warn message if variables are missing

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Set additional variables/Argument/Option settings
SETARGS="$*"
SHORTOPTS=""
LONGOPTS="options,config,version,help,dir:,bg"
ARRAY="default,proxy,static,caddy,go,jekyll,js,php,rails,ruby,python2,python3,netcat"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
setopts=$(getopt -o "$SHORTOPTS" --long "$LONGOPTS" -a -n "$(basename "$0" 2>/dev/null)" -- "$@" 2>/dev/null)
eval set -- "${setopts[@]}" 2>/dev/null
while :; do
  case $1 in
  --options)
    shift 1
    [ -n "$1" ] || printf_blue "Current options for ${PROG:-$APPNAME}"
    [ -z "$SHORTOPTS" ] || __list_options "5" "Short Options" "-$SHORTOPTS" ',' '-'
    [ -z "$LONGOPTS" ] || __list_options "5" "Long Options" "--$LONGOPTS" ',' '--'
    [ -z "$ARRAY" ] || __list_options "5" "Base Options" "$ARRAY" ',' ''
    exit $?
    ;;
  --version)
    shift 1
    __version
    exit $?
    ;;
  --help)
    shift 1
    __help
    exit $?
    ;;
  --config)
    shift 1
    __gen_config
    exit $?
    ;;
  --dir)
    RUN_SERVER_CWD="$2"
    shift 2
    ;;
  --bg)
    shift 1
    echo "launching background process"
    export SERVER_LOG="${RUN_SERVER_TEMP_DIR:-/tmp}/run_server_bg.log"
    $APPNAME "$*" &>"$SERVER_LOG" &
    disown
    exit $?
    ;;
  --)
    shift 1
    break
    ;;
  esac
done
#set -- "$SETARGS"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Actions based on env

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Check for required applications/Network check
cmd_exists --error --ask bash lsof || exit 1 # exit 1 if not found
#am_i_online --error || exit 1     # exit 1 if no internet
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# APP Variables overrides
RUN_SERVER_CWD="$1"
[ -d "$RUN_SERVER_CWD" ] && [ -n "$RUN_SERVER_CWD" ] && shift 1 || RUN_SERVER_CWD="$PWD"
[[ "$RUN_SERVER_CWD" == "$HOME" ]] && printf_exit "Running this in your HOME directory is not supported"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# begin main app
case "$1" in
proxy)
  shift 1
  prog="caddy"
  ARGS="$*"
  main $ARGS
  __proxy || __return_error
  exit $?
  ;;

static)
  shift 1
  prog="static"
  ARGS="$*"
  main $ARGS
  __static || __return_error
  exit $?
  ;;

caddy)
  shift 1
  prog="caddy"
  ARGS="$*"
  main $ARGS
  __caddy || __return_error
  exit $?
  ;;

go)
  shift 1
  prog="go"
  ARGS="$*"
  main $ARGS
  __go || __return_error
  exit $?
  ;;

jekyll)
  shift 1
  prog="jekyll"
  ARGS="$*"
  main $ARGS
  __jekyll || __return_error
  exit $?
  ;;

js)
  shift 1
  prog="http-server"
  ARGS="$*"
  main $ARGS
  __js_serv || __return_error
  exit $?
  ;;

php)
  shift 1
  prog="php"
  ARGS="$*"
  main $ARGS
  __php_serv || __return_error
  exit $?
  ;;

rails)
  shift 1
  prog="ruby"
  ARGS="$*"
  main $ARGS
  __rails_serv || __return_error
  exit $?
  ;;

ruby)
  shift 1
  prog="ruby"
  ARGS="$*"
  main $ARGS
  __ruby_serv || __return_error
  exit $?
  ;;

python2)
  shift 1
  prog="python2"
  ARGS="$*"
  main $ARGS
  __python2_serv || __return_error
  exit $?
  ;;

python3)
  shift 1
  prog="python3"
  ARGS="$*"
  main $ARGS
  __python3_serv || __return_error
  exit $?
  ;;

default)
  shift 1
  prog="python2"
  ARGS="$*"
  main $ARGS
  __default_srv || __return_error
  exit $?
  ;;

netcat | nc)
  shift 1
  prog="netcat"
  ARGS="$*"
  main $ARGS
  __netcat_serv || __return_error
  exit $?
  ;;

*)
  ARGS="$*"
  main $ARGS
  __default_srv || __python3_serv || __python2_serv || __py_simple || __netcat_serv || __return_error
  ;;
esac
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# End application
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# lets exit with code
exit ${exitCode:-$?}
